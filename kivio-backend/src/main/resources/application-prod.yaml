# =============================================================================
# Profile: prod — 本番環境設定
# =============================================================================

spring:
  flyway:
    clean-disabled: true

# 本番では API ドキュメントを公開しない
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
